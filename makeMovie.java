import java.util.*;
/**
 * Write a description of class Movies here.
 *
 * @author (your name)
 * @version (a version number or a date)
 */
public class makeMovie {
    /**
     * Constructor for objects of class Movies
     */
    public void makeMovie(Movie name, ArrayList<Movie> movies) {
        name = new Movie();
        movies.add(name);
    }
}
